version: 2.1

orbs:
  win: circleci/windows@4.1.1

jobs:
  build:
    executor:
      name: win/default
    steps:
      - run: 
         command: $env:CIRCLE_OIDC_TOKEN
         shell: powershell.exe
workflows:
  test:
    jobs: 
      - build:
          context:
            - envar-test
