version: 2.1

setup: true

orbs:
  continuation: circleci/continuation@0.3.1 

jobs:
  generate-config:
    executor: base
    steps:
      - checkout
      - run: 
          name: check tags
          command: |
            echo ${CIRCLE_PULL_REQUEST##*/}

            # CONFIGURATION_PATH=".circleci/continuation.yml"

            LABELS=`curl \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer $GH_TOKEN" \
              https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/issues/${CIRCLE_PULL_REQUEST##*/}/labels`
            LABEL_NAME=`echo $LABELS | jq -c '.[]' | jq '.name'`
            echo $LABEL_NAME

            if [ $LABEL_NAME = "enhancement" ]; then
              CONFIGURATION_PATH=".circleci/tags/tag.yml"
              echo 'export OUTSIDE="some string"' >> "$BASH_ENV"
            fi

            echo 'export CONFIGURATION_PATH="$CONFIGURATION_PATH"' >> "$BASH_ENV"
            echo 'export FINAL_CONFIG_PATH="$CONFIGURATION_PATH"' >> "$BASH_ENV"
            source "$BASH_ENV"
            echo $CONFIGURATION_PATH
            echo $FINAL_CONFIG_PATH
            echo "anything $OUTSIDE"
      - continuation/continue:
            configuration_path: $CONFIGURATION_PATH

workflows:
  setup-workflow:
    jobs:
      - generate-config

executors:
  base:
    docker:
      - image: cimg/base:stable

# VS Code Extension Version: 1.0.0